version: "3"
services:
  postgres:
    image: postgres:15
    restart: always
    ports:
      - "5437:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: postgrespassword
      POSTGRES_USER: postgres
volumes:
  db_data:
